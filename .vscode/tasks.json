{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build and run in docker (dev)",
            "type": "shell",
            "command": "docker build . -t funsearch:dev && docker run -it -v ./data:/workspace/data --env-file .env funsearch:dev funsearch run examples/cap_set_spec.py 11 --sandbox_type ExternalProcessSandbox"
        }
    ]
}